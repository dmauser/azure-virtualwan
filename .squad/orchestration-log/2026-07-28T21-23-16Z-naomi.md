# Orchestration Log — Naomi

**Date:** 2026-07-28T21:23:16Z  
**Session:** paloalto-nw-agent-install  
**Status:** Complete

## Work Summary

Naomi (Infra Dev / enable-monitoring.ps1 owner) added NetworkWatcherAgentLinux installation and fixed idempotency:

### Fixes Delivered

1. **Phase 4b: NetworkWatcherAgentLinux installation:** Added Phase 4b block to `enable-monitoring.ps1` to install `Microsoft.Azure.NetworkWatcher` extension (v1.4, auto-upgrade enabled) on vm-spoke1 and vm-spoke2.

2. **Idempotency probe fix:** Changed probe from `az vm extension show` (fails with ResourceNotFound → stderr terminates under ErrorActionPreference=Stop) to `az vm extension list --query "length([?name=='NetworkWatcherAgentLinux'])"` which exits 0 + returns 0 when absent (safe idempotent check).

3. **Error handling:** Wrapped install in try/catch with WARN + continue on failure (single VM issue does not abort monitoring setup).

### Live Verification

- Installed NetworkWatcherAgentLinux on vm-spoke1 and vm-spoke2 via `enable-monitoring.ps1`
- Verified extension provisioning state: **Succeeded** on both VMs
- `enable-monitoring.ps1` exit code: **0** (success)

### Dependencies Resolved

- Unblocks `validate-flow.ps1` check [2h] (test-connectivity now passes with agent installed)
- Enables full network diagnostic suite (connection monitor, flow logs, connectivity checks)

### Decision Documented

[Decision: Install NetworkWatcherAgentLinux on Spoke VMs in enable-monitoring.ps1](../decisions.md) (2026-07-28, Status: Adopted)

### Git Coordination

- Committed enable-monitoring.ps1 fixes (commit b1230bd)
- Opened PR #14
- Squash-merged to main (merge commit 67812ca) by Coordinator

---

**Status:** Ready for next phase. Spoke VMs now fully instrumented for network diagnostics.
