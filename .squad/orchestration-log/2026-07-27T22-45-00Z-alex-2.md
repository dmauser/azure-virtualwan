# Orchestration Log: alex-2

**Agent:** Alex (Palo Alto Config Specialist)  
**Model:** Sonnet 4.6  
**Session:** 2026-07-27T22:45:00Z  
**Task:** PAN-OS bootstrap fallback + ILB health-probe route fix

---

## Deliverables

1. **scripts/apply-panos-config.ps1** — PowerShell fallback to apply PA config via XML API
   - Invoked post-boot when Azure Files bootstrap fails (SMB blocked by policy)
   - Sends day-0 config from `--template-file` (bootstrap.xml) via HTTPS to PA mgmt IP
   - Idempotent — safe to invoke multiple times
   - Used by Naomi's Phase 7b deploy fallback

2. **scripts/apply-panos-config.sh** — Bash equivalent for Linux labs
   - Identical interface and behavior as PowerShell version
   - Used to validate fix on Linux NVA twin (`nva-spoke-internet/`)

3. **bootstrap.xml enhancement** — Added static route for ILB health-probe fix
   - Route: `168.63.129.16/32 → trust` on ethernet1/2
   - Reason: ILB probes originate from Azure fabric (168.63.129.16); without this route, probe SYN-ACKs exit different NIC, Azure SDN drops as spoofed
   - Impact: Symmetric routing ensures health probes complete, LB marks PA NVA healthy

---

## Routing Note (Debunked UDR Theory)

Initial investigation suspected vWAN hub `ResourceId` next-hop might not propagate ILB IP to spokes. Investigation concluded: **routing is correct**. Evidence:
- Linux NVA lab with identical routing topology passes live egress
- No UDRs present on Linux lab
- Failure was bootstrap-not-applied, not routing

**Implication:** Do NOT add spoke UDRs. Use Phase 7b fallback instead.

---

## Static Validation

✓ `az bicep build templates/nva-paloalto.bicep` → exit 0  
✓ `powershell -NoProfile -Command "& { . scripts/apply-panos-config.ps1 }"` → parse OK  
✓ `bash -n scripts/apply-panos-config.sh` → syntax OK  

---

## Status

**Complete & Shipped.** Ready for Phase 7b integration (Naomi).

---
